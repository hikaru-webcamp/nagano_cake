<div class="container mt-4">
  <h2>ログイン</h2>
  <div class="row">
    <div class="col-md-10 col-md-offset-1">
      <div class= "public-sign-in mt-4">
        <h4>会員の方はこちらからログイン</h4>
        <%= form_with model: @customer, url: customer_session_path, local:true do |f| %>
          <div class="form-group col-11 mx-auto mt-4">
            <%= f.label :email, :"メールアドレス", class: "col-3" %>
            <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "simple@example.com", class: "col-4" %>
          </div>
          <div class="form-group col-11 mx-auto">
            <%= f.label :password, :"パスワード", class: "col-3" %>
            <%= f.password_field :password, autocomplete: "current-password", class: "col-4" %>
          </div>
          <div class="form_group col-9 text-center">
            <%= f.submit "ログイン", class: "btn btn-primary w-25" %>
          </div>
        <% end %>
      </div>
      <div class="public-sign_up mt-4">
        <h4>会員登録がお済みでない方</h4>
        <div class="col-md-11 mx-auto">
          <p><%= link_to"こちら", new_customer_registration_path %>　から新規登録を行ってください。</p>
        </div>
      </div>
    </div>
  </div>
</div>