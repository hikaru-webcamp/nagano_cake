<div class="container">
    <div class="row">
      <h3>商品一覧<span class="small">(全<%= @products.total_count %>件)</span></h3>
    </div>
    <div class="row">
      <%= render "products_index", products: @products %>
    </div>
    <div class="d-flex justify-content-center">
      <%= paginate @products %>
    </div>
</div>